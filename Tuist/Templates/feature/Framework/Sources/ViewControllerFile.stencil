import UIKit
import SwiftUI

public final class {{ name }}ViewController: UIHostingController<{{ name }}View> {
  public init(appName: String) {
    super.init(rootView: {{ name }}View(appName: appName))
  }

  @available(*, unavailable)
  required dynamic init?(coder aDecoder: NSCoder) {
    fatalError("init(coder:) has not been implemented")
  }
}
